# 数字油画 Windows 软件开发总指令

你是本仓库的主开发代理。目标不是做演示，而是持续开发一个可在 Windows 本地运行、最终可打包为 EXE 的“数字油画生产软件”。从现在开始直接进入工程实现，不要只写方案。

## 1. 总目标

输入一张原图后，软件应逐步实现一键生成并导出：

1. 数字油画效果图
2. 编号线稿图
3. 配色图 / 色卡
4. 可编辑矢量 PDF（后续兼容 AI / SVG）
5. 项目文件与质检报告

优先保证生产质量、可编辑性、可重复性和 Windows 本地运行性能。

## 2. 技术方向

- Windows 桌面应用优先使用 Python 3.12 + PySide6。
- 图像处理：OpenCV、Pillow、NumPy、scikit-image。
- 聚类/分色：scikit-learn 或自研色彩量化模块。
- 矢量处理：OpenCV contours + Shapely + SVG/PDF 输出。
- PDF：优先输出真正的矢量路径和可编辑文字，禁止把整页直接栅格化冒充矢量。
- 数据层使用 SQLite / JSON 保存项目、参数、色库、样本与质检结果。
- 结构采用模块化架构，UI 与算法分离，方便后续替换模型或加入 GPU 推理。
- 预留本地 AI 模型接口、ONNX Runtime / DirectML / CUDA 适配层，但第一阶段不能依赖云端才能运行。

## 3. 数字油画第一规则（必须作为硬性验收标准）

### 3.1 效果图

- 颜色尽量贴近原图，不为方便线稿而粗暴简化。
- 色块边界平滑，无像素阶梯和明显锯齿。
- 相邻色块无白缝、透明缝、漏底、错位。
- 每个色块必须唯一归属于一个颜色编号。
- 过小、无意义碎块需要智能合并，但不能破坏主体结构和最终观感。
- 无法容纳约 5pt 编号的小色块应进入“合并/优化”候选，而不是直接留下不可生产碎块。

### 3.2 线稿图

- 线宽固定目标：0.1 pt。
- 线条颜色目标：CMYK 40,100,100,100。
- 小/中/大编号字号目标：4.2 pt / 6 pt / 8 pt。
- 相邻色块共享同一条边界，只允许一条线，禁止双线、重叠线、交叉线和残留碎线。
- 所有区域必须闭合、连续、平滑。
- 放大检查仍不能有毛刺、阶梯锯齿、抖动折线。
- 线稿区域轮廓、数量、相邻关系、边界位置必须与效果图严格对应。
- 数字必须放在自己的封闭区域内部，禁止越界、盖在线上、放到错误色块。

### 3.3 配色图

- 使用项目自有色库编号体系，不要把临时聚类序号当正式色号。
- 色卡中的编号、色值、效果图区域、线稿区域必须一一对应。
- 支持导入/编辑自有色库，至少保存：色号、RGB、HEX、Lab；后续可扩展 CMYK。

### 3.4 矢量输出

- 路径应可编辑，文字编号保持文字对象或独立矢量对象。
- 不允许整页栅格化后写进 PDF。
- 共享边界避免重复描边。
- 导出的效果图、线稿图、配色图必须使用同一套区域 ID 和颜色 ID。

## 4. 软件核心模块

请按以下模块实施，而不是把所有逻辑写在一个文件中：

- `app/`：PySide6 UI、窗口、项目管理
- `core/image/`：预处理、降噪、边缘保持、颜色空间
- `core/segmentation/`：分区、区域图、邻接关系
- `core/palette/`：颜色量化、自有色库匹配、色差计算
- `core/regions/`：碎块检测、合并、闭合、拓扑修复
- `core/vector/`：轮廓提取、曲线平滑、共享边界、SVG/PDF
- `core/labels/`：编号位置、字号选择、防越界
- `core/qc/`：自动质检
- `core/export/`：PNG/SVG/PDF/项目包
- `data/`：默认色库、配置、样本元数据
- `tests/`：自动测试

## 5. 自动质检必须逐步实现

至少检测：

- 白缝 / 透明缝 / 漏底
- 未闭合区域
- 重复边界 / 双线
- 交叉线
- 极小碎块
- 无法容纳编号的区域
- 编号越界
- 编号与颜色不一致
- 效果图与线稿区域数量不一致
- 色库映射遗漏
- 轮廓锯齿、过密锚点、异常尖角
- 导出 PDF 是否仍含真实矢量对象

所有 QC 项应输出 PASS/WARN/FAIL 和定位信息。

## 6. 持续学习数据结构

实现可控的案例学习，而不是让模型无约束即时改权重：

- `reference_samples`：用户确认优秀的原图 + 效果图 + 线稿 + 矢量成品
- `negative_samples`：错误结果
- `corrections`：用户指出的问题及修正版
- `parameter_profiles`：从不同作品沉淀的参数模板

后续优先采用：案例检索（RAG 思路）+ 规则评分 + 参数自适应；模型微调留作后期能力。

## 7. UI 第一阶段必须包含

- 导入原图
- 图片预览，支持缩放与拖动
- 目标色数选择（例如 12/24/36/50，自定义）
- 导入/选择自有色库
- “生成效果图”
- “优化碎块”
- “生成线稿”
- “生成配色图”
- “自动质检”
- 导出 PNG / SVG / PDF
- 参数面板与进度/日志区

UI 操作不能卡死主线程；耗时任务使用 QThread / QRunnable 或等价后台 worker。

## 8. 开发顺序

### V0.1：可运行骨架

立即完成：

1. 创建标准项目结构与依赖文件。
2. PySide6 主界面可启动。
3. 支持导入 JPG/PNG 并预览。
4. 支持选择目标色数。
5. 实现基础 Lab/KMeans 分色，生成第一版效果图。
6. 能把结果保存为 PNG。
7. 写最小单元测试与 README 运行说明。

### V0.2：区域生产化

- 连通区域分析
- 小碎块合并
- 区域邻接图
- 边界平滑
- 无缝区域重建

### V0.3：线稿和编号

- 共享边界矢量化
- 0.1pt 线稿
- 编号放置算法
- 4.2/6/8pt 自适应字号

### V0.4：自有色库

- Lab 色差匹配
- 色号管理
- 配色图模板

### V0.5：矢量 PDF + QC

- 三页/三栏可编辑矢量 PDF
- 自动质检报告

后续再迭代 GPU、模型辅助、训练/参考样本库和 AI 文件兼容。

## 9. 开发纪律

- 每个版本必须是“可以运行的增量”，不要一次写大量不可运行伪代码。
- 所有关键算法给出类型标注、docstring 和测试。
- 不要用 TODO 代替核心实现。
- 不要删除已经可工作的能力来换取表面上的重构。
- 每完成一个版本，更新 `CHANGELOG.md`。
- README 必须给出 Windows 安装、运行、测试、打包步骤。
- 优先 CPU 可运行；GPU 是加速项，不是基本功能前提。

## 10. 现在立即执行

不要继续输出架构讨论。直接从 V0.1 开始创建代码，至少完成：项目结构、依赖、PySide6 主窗口、图片导入预览、目标色数设置、Lab/KMeans 基础分色、结果预览与 PNG 导出、测试和 README。

当 V0.1 可运行后，再继续 V0.2，不要停留在仅有文档的状态。
